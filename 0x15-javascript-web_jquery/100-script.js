document.addEventListener('DOMContentLoaded', function() {
    // Select the <header> element
    var header = document.querySelector('header');
    
    // Update the text color of header to red (#FF0000)
    header.style.color = '#FF0000';
});
